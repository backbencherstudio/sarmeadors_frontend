import React from 'react'

export default function EditChildrenInformation({ open, setOpen }: { open: boolean, setOpen: (open: boolean) => void }) {
    return (
        <div>EditChildrenInformation</div>
    )
}
