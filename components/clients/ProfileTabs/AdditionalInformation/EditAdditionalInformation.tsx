import React from 'react'

export default function EditAdditionalInformation({ open, setOpen }: { open: boolean, setOpen: (open: boolean) => void }) {
    return (
        <div>EditAdditionalInformation</div>
    )
}
